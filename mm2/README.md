# bleed.rest — mm2

MM2-clone module (PlaceId `142823291`). Loaded by the hub `loader.lua`,
or standalone:

```lua
loadstring(game:HttpGet("https://raw.githubusercontent.com/pandaeatdonuts-byte/bleed.rest/main/mm2/main.lua"))()
```

## Contents

- `main.lua` — kill aura, coin farm, ESP, movement, 1v1 queue

Menu key: **RightShift**.
